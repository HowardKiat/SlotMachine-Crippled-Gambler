# SlotMachine-Crippled-Gambler
Slot Machine

## Project Setup

First node.js ("https://nodejs.org/en/download") must be installed

Once installation is done proceed by running the following command:
```
npm init
```
```
npm i prompt-sync
```
